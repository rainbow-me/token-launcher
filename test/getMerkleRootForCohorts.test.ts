import { getMerkleRootForCohorts } from '../src/api';

describe('Get Merkle Root For Cohorts', () => {
  it('should get the merkle root for cohorts', async () => {
    // const merkleRoot = await getMerkleRootForCohorts(['0x1234567890123456789012345678901234567890']);
    const foo = 1;
    expect(foo).toBeDefined();
  });
});
